name: CI Testing

on: 
 - push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run Tests
      run: |
        echo 'cd to app/UCSBB/'
        cd app/UCSBB/
        npm install jest
        ls
        echo 'cd to app/UCSBB/'
        cd app/UCSBB/
        echo 'made it'
        ls
        npm run test
